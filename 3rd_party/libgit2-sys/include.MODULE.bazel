bazel_dep(name = "libgit2", version = "1.9.1")

crate = use_extension("@rules_rs//rs:extensions.bzl", "crate")
crate.annotation(
    crate = "libgit2-sys",
    gen_build_script = "off",
    deps = ["@libgit2"],
)

inject_repo(crate, "libgit2")
